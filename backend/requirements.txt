Flask>=3.1
flask-cors>=6.0
python-dotenv>=1.2
# Pinned exactly: tests assert on the request shapes these SDKs are handed
# and on the JSON schema pydantic emits.
openai==3.16.2
google-genai==2.24.0
pydantic==2.13.5
black==26.5.1
pytest>=8.0
