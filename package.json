{
  "name": "unilens-serve",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@types/auto-bind": "^1.2.0",
    "auto-bind": "^5.0.1",
    "chokidar-cli": "^3.0.0"
  },
  "devDependencies": {
    "concurrently": "^10.0.4",
    "live-server": "^1.2.2"
  }
}
