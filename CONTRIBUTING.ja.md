# UniLens への貢献

> [CONTRIBUTING.md](CONTRIBUTING.md) の日本語訳です。規約の正典は英語版であり、両者が食い違った場合は英語版が優先されます。

このリポジトリの規約です。コードベース全体が一つの声で読めるようにするためのものなので、
他のプロジェクトの習慣を持ち込まず、ここに合わせてください。

## 0: 言語

プロジェクトの主要言語は英語です。日本語によるドキュメントやコメントの貢献については、以下のガイドラインに従ってください：

* 英語の `.md` マークダウンファイルの日本語訳には、次のように `.ja.md` のサフィックスを付けることができます：

```
README.md
README.ja.md     // 日本語訳

```

* インラインの英語コメントの日本語訳には、次のように `ja:` のプレフィックスを付けることができます：

```
// The architecture of this file is XYZ.
// ja: このファイルのアーキテクチャはXYZです。

```

## 1. アーキテクチャ: React と命令的 DOM

どちらのライブラリも埋め込み型ウィジェットであり、2 種類のコードから成り立っています。
その境界は意図的なものです。

- **React** — ビューと同期し続ける必要のある状態を持つもの。パネル、ポップオーバー、
  設定 UI がこれにあたります。例: `ChatPopover`、`SettingsPanel`、`DebugPanel`。
  宣言的なレンダリングが、手書きの DOM パッチ・フォーカスの管理・再描画のバッチ処理を
  肩代わりします。
- **命令的 DOM** — フレームループで動くもの、あるいは *ホストページの* DOM を書き換える
  もの。ズームエンジン、ドラッグのラバーバンド、ズームバッジ、ミニマップ、ヒントチップ、
  キャプチャパイプラインがこれにあたります。mousemove ごとの style 書き込みに React は
  何も足しませんし、自分が所有していない DOM を React に管理させてはいけません。

目安: *ビューと同期する状態を持つなら React、フレームループやホストの DOM を持つなら
命令的。* 短命なオーバーレイを React に移植しないこと。状態を持つパネルを手書きしないこと。

React のルートは `document.documentElement` 直下の専用コンテナ（`#unilens-root`、
`#unilens-settings-root` など）へ遅延マウントします。ズームで transform される `body` の
外側にあり、キャプチャからも除外されます。

## 2. 状態と永続化

ライブラリごとに zustand のストアを 1 つ、ストアモジュール
（`unilens-lib/src/settings.ts`）に定義します。

```ts
export const useSettings = create<Settings>()(persist(() => ({ ...DEFAULTS }), { name: 'unilens-settings' }))
export const getSettings = () => useSettings.getState()
export const onSettingsChange = (cb: () => void) => useSettings.subscribe(cb)
```

- React コンポーネントは `useSettings()` フック（またはセレクタ）で購読します。
- 命令的モジュールは `getSettings()` でライブな状態を読みます。ユーザーが途中で設定を
  変えうる場合、`await` をまたいでスナップショットをキャッシュしないこと。
- 永続化は `persist` ミドルウェアだけを通します。他のどこでも生の `localStorage` を
  呼ばないこと。新しく永続化したい値は、新しいキーを増やすのではなく、ストアの
  フィールドにします（`pinnedPos` を参照）。
- ストレージキーには `unilens-` を前置します。
- 開発中は後方互換のマイグレーションを行いません。フォーマットを変えた結果、保存済みの
  値がデフォルトに戻ってもかまいません。
- ハイドレートした値でレイアウトや計算が壊れうるなら、それを使う側で検証します
  （座標の有限性チェック、数値の範囲チェックなど）。

## 3. ホストページの一員としての振る舞い

ライブラリは任意のホストページの中で動きます。ホストは我々が客として入り込んだ領域だと
考えてください。

- **ホストの状態を壊さない。** style や属性を上書きするなら、元の値を保存して復元します。
  取り消しは自分が付けたマーカーの削除であるべきで、ホストの状態を組み立て直すことでは
  ありません。
- **すべてに接頭辞を付ける。** 我々が追加する DOM の id・class・data 属性には `unilens`
  を前置します（`data-unilens-img`、`#unilens-settings-root`）。
- **補間する値はエンコードする。** URL のパスやクエリに入る可変値は、例外なく
  `encodeURIComponent` を通します。
- **document レベルのリスナーで状態を漏らさない。** 相互作用の状態機械（ドラッグ、抑止
  フラグ）には、ついに届かなかったイベントからの復帰経路が必要です。次の `mousedown` で
  リセットする、`window` の `blur` でキャンセルする、`e.buttons === 0` で取りこぼした
  `mouseup` を検出する、といった手当てです。
- **ソフトに失敗する。** ストレージのブロック、バックエンドの不在、想定外のページ構造は
  機能を縮退させるだけで、イベントハンドラの外へ例外を投げてはいけません。

## 4. TypeScript とビルド

- `strict` TypeScript。ビルドは型チェックに依存します（`make build` は先に `tsc -b` を
  走らせます。esbuild は型を検査せずに落とすだけなので）。
- リテラルを複製せず、型から導出します。キーの一覧が既に型にあるなら、計算で求めます。

  ```ts
  type BoolSettingKey = { [K in keyof Settings]: Settings[K] extends boolean ? K : never }[keyof Settings]
  ```

- 各ライブラリは esbuild で単一の dist ファイルにバンドルします（`make build`）。
  モジュールごとの出力は行いません。

## 5. コメント

**英語**で書きます。

- **既定は why。** コメントはコードが語れない根拠を運びます。そのガードが防いでいるバグ、
  その分岐が回避しているブラウザの癖、より単純な手を採らなかった理由です。
- **"what" のコメントは、コードが文章より読みにくいときだけ。** 正当な形は 3 つです。
  - 長いファイルのセクションバナー: `// ── Alt+drag region select ──`
  - 密なロジック（座標計算、正規表現）の 1 行要約
  - 公開 API への doc コメント: `/** live snapshot for non-React modules */`
- 自明なコードを言い換えないこと（`flag = true` の上に `// set flag` と書かない）。
- 一つの関心につき一つのコメント。コードが変わったコメントは、古びるに任せず削除します。
- 意図的な単純化で既知の上限があるものには、その上限と将来の移行先を書いたコメントを
  添えます。例:
  `// 24MP canvas cap guards very long pages; switch to tiled rendering if it ever bites`
- そのファイルの既存の密度に合わせます。このコードベースは意図してコメントが多めですが、
  それでも 1 行ごとに存在意義が必要です。

## 6. コミットと PR

- Conventional な接頭辞を使います: `feat:`、`fix:`、`refactor:`、`chore:`、`docs:`。
- 件名は命令形で、内部の仕組みではなくユーザーから見た効果を書きます:
  `fix: do not submit chat input on IME-composition Enter`。
- 本文にはその変更が正しい**理由**を、必要に応じて検証方法も書きます。
- 1 コミット 1 論理変更。リファクタ中に見つけたレビュー指摘の修正は、無関係な作業に
  混ぜ込まず独立したコミットにします。
- `main` への PR は `f/` 接頭辞の feature ブランチから出します。

## 7. ビルドシステム

- どのパッケージも同じ Make の動詞を公開します: `init`、`clean`、`build`、`serve`
  （JS ライブラリの場合）。該当するものには `lint` / `test` も加えます。
- 新しい JS / フロントエンドのターゲットは `make-targets.mk` に宣言します
  （`JS_TARGETS`、`target_dist_*`、`frontend_port_*`）。ルートの `Makefile` がそれらを
  回すので、レシピの中にターゲット一覧をハードコードしないこと。

## 8. 依存関係

次のはしごを登り、最初に成立した段で止まります。

1. プラットフォームの機能（JS ではなく CSS、resize リスナーではなく `max()` / `dvh`、
   ヘルパーではなく DOM API）。
2. すでにコードベースにあるもの — 書く前に探すこと。
3. すでに入っている依存。
4. 新しく入れる、小さく用途の絞られた依存（zustand 級: 約 1KB、仕事は一つ）。
   PR の説明で正当化すること。

単一の機能のためにフレームワーク規模のものを足さないこと。バンドルサイズは重要です。
これらのライブラリは、他人のページに `<script>` で埋め込まれます。
